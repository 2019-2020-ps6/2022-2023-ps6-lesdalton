/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GameSelectPlayerComponent } from './game-select-player.component';

describe('GameSelectPlayerComponent', () => {
  let component: GameSelectPlayerComponent;
  let fixture: ComponentFixture<GameSelectPlayerComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GameSelectPlayerComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GameSelectPlayerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
