/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { QuizConfigQuestionComponent } from './quiz-config-question.component';

describe('QuizConfigQuestionComponent', () => {
  let component: QuizConfigQuestionComponent;
  let fixture: ComponentFixture<QuizConfigQuestionComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ QuizConfigQuestionComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(QuizConfigQuestionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
