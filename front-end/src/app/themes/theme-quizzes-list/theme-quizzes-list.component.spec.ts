/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ThemeQuizzesListComponent } from './theme-quizzes-list.component';

describe('ThemeQuizzesListComponent', () => {
  let component: ThemeQuizzesListComponent;
  let fixture: ComponentFixture<ThemeQuizzesListComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ThemeQuizzesListComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ThemeQuizzesListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
