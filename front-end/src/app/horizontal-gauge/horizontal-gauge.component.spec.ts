/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { HorizontalGaugeComponent } from './horizontal-gauge.component';

describe('HorizontalGaugeComponent', () => {
  let component: HorizontalGaugeComponent;
  let fixture: ComponentFixture<HorizontalGaugeComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ HorizontalGaugeComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(HorizontalGaugeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
