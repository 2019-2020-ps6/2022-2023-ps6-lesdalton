FROM mcr.microsoft.com/playwright:v1.35.0-jammy

ARG ENVIRONMENT

USER pwuser

WORKDIR /app

COPY --chown=pwuser:pwuser package.json package-lock.json ./
RUN npm install --no-cache

COPY --chown=pwuser:pwuser . .

# Copiez le fichier de configuration dans le conteneur
COPY playwright.config.ts .

# Exécutez les tests en utilisant la configuration
CMD ["npm", "run", "test:e2e", "--config=./playwright.config.ts"]

COPY --chown=pwuser:pwuser ./test-results /app/test-results
