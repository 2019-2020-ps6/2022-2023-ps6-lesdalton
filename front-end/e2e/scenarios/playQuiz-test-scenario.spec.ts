/*test('Test de navigation de page à page', async ({ page }) => {
  await page.goto('https://example.com');

  const link = await page.locator('a');
  await link.click();

  await page.waitForNavigation();

  const newUrl = page.url();
  expect(newUrl).toBe('https://example.com/new-page');

  // Effectuez d'autres actions et assertions sur la nouvelle page
});*/
