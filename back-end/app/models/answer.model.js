const Joi = require('joi')
const BaseModel = require('../utils/base-model')

const answerSchema = new BaseModel('Answer', {
  text: Joi.string().required(),
  isCorrect: Joi.boolean().required(),
})

module.exports = answerSchema
const Joi = require('joi')
const BaseModel = require('../utils/base-model')

const answerSchema = new BaseModel('Answer', {
  text: Joi.string().required(),
  isCorrect: Joi.boolean().required(),
})

module.exports = answerSchema
const Joi = require('joi')
const BaseModel = require('../utils/base-model')

const answerSchema = new BaseModel('Answer', {
  text: Joi.string().required(),
  isCorrect: Joi.boolean().required(),
})

module.exports = answerSchema
const Joi = require('joi')
const BaseModel = require('../utils/base-model')

const answerSchema = new BaseModel('Answer', {
  text: Joi.string().required(),
  isCorrect: Joi.boolean().required(),
})

module.exports = answerSchema
const Joi = require('joi')
const BaseModel = require('../utils/base-model')

const answerSchema = new BaseModel('Answer', {
  text: Joi.string().required(),
  isCorrect: Joi.boolean().required(),
})

module.exports = answerSchema
const Joi = require('joi')
const BaseModel = require('../utils/base-model')

const answerSchema = new BaseModel('Answer', {
  text: Joi.string().required(),
  isCorrect: Joi.boolean().required(),
})

module.exports = answerSchema
const Joi = require('joi')
const BaseModel = require('../utils/base-model')

const answerSchema = new BaseModel('Answer', {
  text: Joi.string().required(),
  isCorrect: Joi.boolean().required(),
})

module.exports = answerSchema
const Joi = require('joi')
const BaseModel = require('../utils/base-model')

const answerSchema = new BaseModel('Answer', {
  text: Joi.string().required(),
  isCorrect: Joi.boolean().required(),
})

module.exports = answerSchema
